Java classfile formatter / decompiler / compiler using python object notation.

Java classfile format is decompiled into python objects (dicts and arrays),
then pretty printed to show a decompiled view of a class.

The text output can then be modified an compiled again.

usage:
 classfile.py -d <classfile>
 classfile.py -c <pyobjfile> <classfile>
